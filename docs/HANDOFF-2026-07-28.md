# Handoff — Sherman Abrams Agent — 2026-07-28

## 1. What Was Just Finished

- **Summary of completed work:** (all committed AND pushed; `main` = `origin/main` = `045ca78`)
  - v6.1 `45ae8b9` — tall launch panel + mid-turn self-talk (dim ⋯ lines from real codex reasoning items; injected `model_reasoning_summary="auto"` only when user hasn't set it).
  - v7 `ccccb91` — slash commands `/goal /plan /subagent /help` + CommandMenu; isolated read-only ephemeral workers: every inherited MCP server disabled by name (parsed from TOML section headers ONLY, never values) plus `orchestrator.mcp.enabled=false` fail-closed; first `shell/test/` suite.
  - v8 `e253238` — responsive chrome + isolated commands (committed outside Cowork's view, verified after the fact).
  - v9 `74e110a` — rounded bordered composer (pink 205), chip status strip, in-border `╭─ Sherman ─╮` reply label. NOTE: chips are the agent's OWN design — real Hermes uses a plain `│`-joined line; only the in-border label is a genuine Hermes port (spec w/ file:line cites in the dispatch scratchpad `hermes-chrome-spec.md`).
  - v10 `740b62f`+`6c4c2d7` — panel TALL_SHARE 0.75 (~37 rows at 120×60); top-anchor already existed, now pinned by test; Hermes band-sweep load-in (`shell/src/ui/loadin.js`, raw escapes pre-Ink, TTY-gated no-op off-TTY, labels wrap the actual awaits, `done()` sync in `finally`, budget 1s).
  - `d283192` — mark scales 2× nearest-neighbour in tall mode (Hermes has no scaling technique to copy; terminals can't scale glyphs).
  - `f4c2dd9` — transcript scrollback incl. mid-turn: clipped-viewport negative-margin technique (stock Ink has no ScrollBox; renderToString mid-render = nested React render, rejected). PgUp/PgDn + shift+arrows; "viewing history — N lines below" from real Yoga measurements, `unknown` when unmeasured.
  - `045ca78` — mouse: SGR 1006 on while mounted, OFF on all exit routes via `writeSync` on raw fd (exit+SIGINT/SIGTERM/SIGHUP, re-raised not swallowed); click places composer caret, wheel scrolls; composer gained insert-at-caret (new editing behavior, mouse-only so far).
  - Cowork sandbox: file deletion enabled for sherman folder (kills the index.lock dead-end); sandbox commits use `-c user.name="Evan Motovich" -c user.email=evanmotovich1@gmail.com` (no git config in sandbox).
- **Key changes or features added:** new files `shell/src/commands.js`, `shell/src/ui/CommandMenu.js`, `shell/src/ui/loadin.js`, `shell/src/ui/mouse.js`, `shell/test/` (5 files); `.claude/` gitignored.
- **Unresolved edge cases:**
  - Load-in animation NEVER watched on a real pty (both sandboxes lack one) — needs one `sherman` launch + eyeball.
  - Real-terminal shakedown pending: scrollback mid-turn, click-caret typing, exit leaves terminal clean (no `[<0;7;13M` garbage after exit = mouse mode really off).
  - Claude engine backend still a stub — `/model` can't offer Claude models until it's real.
  - No live resize reflow; keystrokes dead under bare `expect` pty (harness artifact, fine in real terminals).

## 2. Working Folders

- **Main development folder(s):** `/Users/moto/code/sherman` (HEAD `045ca78`, clean, pushed). Remote: https://github.com/evanmotovich1-web/sherman (private, LIVE — top "no remote" risk closed this session).
- **Supporting assets/config folders:** `~/.sherman/` (config.json, sessions/); vault at `vault/` (still 1 wiki page, 0 shared facts); Hermes parts donor at `~/.hermes/hermes-agent/ui-tui/src/`; `/Users/moto/code/second-brain/sessions/` (Sherman session digests).
- **Temporary or scratch work folders:** dispatch scratchpad `hermes-chrome-spec.md` (wherever the v9 session wrote it) — reference, safe to lose.

## 3. Mindset / Reasoning

- **Why certain approaches were chosen:**
  - Evan now dispatches Sherman work via HERMES and Claude Code /goal prompts; Cowork's job is verify-then-commit. Reports OVERSTATE: v7 claimed "diff clean"+"12/12" while tree was uncommitted and check 10 failing; v9 folded 8 pre-dirty files into its commit. Always: rerun `bash smoke.sh` + `node --test test/*.test.js`, read the load-bearing diffs (smoke.sh first — a loosened check is a fail).
  - Scrollback = negative-margin clip because stock Ink lacks Hermes' forked ScrollBox; component tree identical at every offset ⇒ buffer is literally what rendered.
  - Chips kept although not-Hermes: Evan liked the result; don't cite them as "the Hermes look".
- **Known trade-offs or constraints:**
  - Honesty laws remain load-bearing and smoke-enforced: only real engine events, meter only from reported usage, no invented labels; load-in labels wrap actual awaits.
  - Brand: pink 205 / purple 135 / blue 39; red retired (smoke check 9). Evan has approved green as a SEMANTIC meter color and red as SEMANTIC diff color (scoped, check 9 stays strong on brand surfaces) — in the drafted dispatch, not yet built.
  - smoke check 10 (`sherman update`) fails in the Cowork sandbox (no GitHub creds) — env, passes on Mac. `node --test test/` bare-dir invocation fails; use `test/*.test.js`.
- **Mental notes for the next session:**
  - DRAFTED, NOT YET RUN — next dispatch (in chat, Evan has it): 4 tasks = arrow-key caret movement · green meter + % with investigate-first on interim usage events · transient live diff green/red with investigate-first on file_change payload + surgical check-9 scoping · `/model` + `/effort` (codex-only until claude backend exists, fresh thread on switch).
  - Sherman ran its FIRST real production ops task (agentic-os CTO-gate rework loop, 44 min unattended): pushed `f3f7e90`+`4b3cf95` to agentic-os, 622 tests floor 569, board triggers live, correctly REFUSED to sync the live `agentic-manager` skill (unauthorized). Loose ends live in agentic-os, not here: gateway + Pi dispatcher not restarted; skill sync awaits Evan's go; session HTML uncommitted; second-brain digest at `/Users/moto/code/second-brain/sessions/agentic-os/2026-07-28-cto-rework-review-loop.md` (commit/push UNVERIFIED).
  - Write-scope fact confirmed live: workspace-write covers cwd ⇒ `sherman` launched inside a repo can mutate and push it. On-record decision while Evan is sole user; flips to blocker at employee onboarding.
  - Evan STILL owes the 3–5 real lab tasks to seed skills + vault. Chrome is done; this is the highest-value next input.

## 4. What's in Production

- **Current production version:** Evan's Mac only; `sherman` on PATH (symlink → repo, new launches pick up new code; running sessions don't). v0.2.0 · `045ca78`, engine codex / gpt-5.6-sol, user evan-motovich.
- **Deployment date/time:** pushed to origin 2026-07-28 (UNVERIFIED exact time).
- **Known production issues or limitations:** single-user; vault local-only; claude backend stub; WhatsApp/server/installer phases not started (plan §6 in agentic-os `plans/PLAN-2026-07-25-sherman-abrams-agent.md`).

## 5. Other Relevant Info

- **Dependencies or integrations to watch:** codex CLI (probed against 0.145.0 — reasoning summary opt-in semantics live there); Evan's `~/.codex/config.toml` MCPs (computer-use, node_repl, obsidian) inherited by normal turns, disabled by name in isolated workers; Node only runtime.
- **Testing status:** `bash smoke.sh` from repo root — 14 checks (check 10 needs GitHub creds/network); `cd shell && node --test test/*.test.js` — 41/41. Both green at HEAD. AGENTS.md rule: smoke before commit, `graphify update .` after (graph: 573 nodes / 782 edges).
- **Links:** repo https://github.com/evanmotovich1-web/sherman · plan of record `/Users/moto/code/agentic-os/plans/PLAN-2026-07-25-sherman-abrams-agent.md` (§6b decisions log) · prior handoff `docs/HANDOFF-2026-07-27.md`.

**Next session, start here:** → Launch `sherman` in a real terminal and shake down the untested surfaces (load-in animation, mid-turn PgUp, click-caret, exit → mouse mode off); then paste the drafted 4-task UI dispatch — and get the 3–5 lab-task list out of Evan to start seeding skills + vault.
