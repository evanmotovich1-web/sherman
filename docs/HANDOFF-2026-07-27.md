# Handoff — Sherman Abrams Agent — 2026-07-27

We are MOVING FAST: this project went from empty folder to a branded,
Hermes-class terminal agent in ~48 hours. Keep that pace but keep the
verify-before-commit loop — it caught real bugs every round.

> **Superseded 2026-07-28:** The v6.1 tall panel described below was committed
> in `45ae8b9` and then deliberately reversed by the current launch/chrome
> rewrite. Launch cards now use compact/full content-hugging modes so spare
> terminal rows remain available to the transcript. Do not follow this
> handoff's instructions to verify the old six-file v6.1 diff or treat the
> chrome as finished; verify and record the current rewrite instead.
> The v7 baseline is committed through `ccccb91`, and `origin/main` exists.
> This historical handoff is not an operational source for the current HEAD;
> use `git log`, `git status`, `CHANGES.md`, and the repository tests instead.

## 1. What Was Just Finished

- **Full product chassis through shell v7, committed through `ccccb91`:**
  - `bin/sherman` launcher: wizard (Anthropic/OpenAI OAuth choice = engine
    choice, engine's native login does the OAuth), config at
    `~/.sherman/config.json`, adapter assembly, `sherman update`, `--raw`.
  - Sherman Shell (`shell/`, Node + Ink): engine layer (`EngineSession`,
    codex backend live via `codex exec --json` + `resume`, claude backend
    stub), full-bleed launch screen, SHERMAN-AGENT retro-3D lockup
    (construction copied cell-for-cell from Hermes `banner.ts`), brand
    rebrand pink 205 / purple 135 / blue 39 (red retired, smoke-enforced),
    live turn UI (prompt → factual activity → signed reply),
    context meter from real `turn.completed.usage.input_tokens` vs 272K
    window, session identity (id, JSONL log at `~/.sherman/sessions/`,
    memory attribution line "— user · session · date"), version header,
    alternate-screen mode with restore on every exit path, viewport
    transcript (no `<Static>`).
  - Vault at `vault/` (wiki/, memory/shared/, memory/private/<user>/,
    inbox/): Sherman wrote its first wiki page (self-audit improvement
    plan). Launch panel counts are live.
  - Docs: AGENTS.md (86 lines, per-commit graphify rule), DESIGN.md,
    CHANGES.md, docs/OBSIDIAN-SETUP.md, .mcp.json (Obsidian + graphify).
  - smoke.sh: 13 checks, all green, no framework. Checks are load-bearing
    (full-bleed width, brand colors present + red absent, turn structure,
    honest `sherman update`).
- **SUPERSEDED WORK:** v6.1's stretched panel was committed and then reversed.
  The 2026-07-28 launch/chrome rewrite adds content-hugging launch modes,
  two-row persistent chrome, slash commands, isolated read-only workers,
  terminal sanitization, and responsive live-render tests. See `CHANGES.md` and
  current Git history for its final commit state; there is no six-file v6.1
  diff left to verify.
- **Unresolved edge cases:** no page-up scroll of scrolled-off turns
  (README-documented); East Asian Ambiguous double-width terminal profiles;
  keystrokes dead under bare expect pty (harness artifact, fine in real
  terminals); claude engine backend still a stub.

## 2. Working Folders

- **Main:** `/Users/moto/code/sherman` — the whole product. Shell v7 baseline
  `ccccb91`; an `origin/main` remote exists. Inspect live Git state rather than
  relying on this historical handoff for the current commit.
- **Supporting:** `/Users/moto/code/agentic-os` — design docs live at
  `plans/PLAN-2026-07-25-sherman-abrams-agent.md` (the plan of record:
  phases, hidden-vault memory model, decisions log §6b) and
  `plans/PLAN-2026-07-25-t6a-model-tiering.md` (factory work, separate).
- **Supporting:** `/Users/moto/code/second-brain` — Evan's personal vault;
  `sessions/sherman/` holds agent session digests. Unrelated to the
  Sherman company vault.
- **Scratch:** `/tmp/ui-skills` clone (safe to delete). `.hermes/` dirs in
  repos are agent-session metadata — gitignored in sherman, stray in
  agentic-os.

## 3. Mindset / Reasoning

- **Sherman rides Codex/Claude Code, no own engine** — auth = engine
  choice, zero OAuth code owned.
- **Hermes is the design bar and the parts donor** — wordmark construction
  read from `~/.hermes/hermes-agent/ui-tui/src/banner.ts`, not guessed
  from screenshots. Copy source when source exists.
- **Honesty rules are load-bearing:** UI renders only real engine events
  (no fake activity lines), meter only from reported usage, panel shows
  only true content. Enforced by smoke, not prose.
- **Decisions ON RECORD (do not re-raise while Evan is sole user):**
  Sherman inherits Evan's machine MCPs/reads (wanted); raw session logs
  stay unredacted. BOTH flip to blockers at employee onboarding — see
  plan §6b + Sherman's own audit in vault/wiki.
- **~20K token/turn floor is the harness** (persona + codex + MCP tool
  schemas), mostly cache-hit. Lever if wanted: clean engine config with
  fewer MCPs.
- **GitHub-repo shopping verdict:** Charmbracelet Crush (installed via
  brew) = design reference only; markdown rendering in the reply box is
  the last worthwhile UI feature (Glow as the bar); ibelick/emil skills
  are for Claude Code web-UI work, NOT Sherman. Installing repos never
  changes Sherman — only dispatches do.
- **git index.lock collisions (4 this session):** open agent sessions
  poll git. Habit: close finished sessions before committing, or let the
  working session commit itself. Stale lock = 0 bytes + old mtime + no
  git process → safe to rm.
- **Ultracite was run by accident in BOTH sherman (cleaned) and
  agentic-os `agent-workflows/` (NOT cleaned — biome.jsonc, package.json,
  node_modules/ sitting untracked there).**

## 4. What's in Production

- "Production" = Evan's Mac only. `sherman` on PATH via install.sh;
  engine codex / gpt-5.6-sol; user evan-motovich.
- Version: v0.2.0; shell v7 baseline `ccccb91`, with later rewrite state recorded
  in current Git history.
- Limitations: single-user, vault local-only, WhatsApp/server/installer
  phases not started (plan §6: Phase 2 installer + second admin device,
  Phase 3 always-on box + vault service + WhatsApp + employees).

## 5. Other Relevant Info

- **Tests:** `./smoke.sh` from repo root — 13 checks, must be green
  before any commit (AGENTS.md rule; also run `graphify update .` after).
- **Obsidian:** vault has NO `.obsidian/` yet — Evan must open
  `/Users/moto/code/sherman/vault` as an Obsidian vault; then optional
  Local REST API plugin per docs/OBSIDIAN-SETUP.md (key via env var).
  After first open: gitignore `vault/.obsidian/workspace*.json`.
- **Dependencies:** Node (only runtime), codex CLI, Evan's ~/.codex MCPs
  (Sherman inherits them — deliberate).
- **The vault is the product now:** company knowledge and skills remain the next
  value layer after the current chrome rewrite is verified and committed. Evan
  still owes the list of 3–5 real lab tasks to seed the first skills.

**Next session, start here:** → Ignore the obsolete v6.1 instructions above.
Inspect current Git history/status and `CHANGES.md`, run the complete current
gate, and review the latest commit. Do not create a new remote; `origin/main`
already exists. Evan reviews and pushes under repository policy.
