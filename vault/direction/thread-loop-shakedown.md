status: open
next: run `sherman loop 3` supervised and see whether picks sized to the cap complete and compound across iterations
evidence: first live run 2026-08-13 — pick logged before execution as designed, but the pick ("implement an improvement end-to-end with tests and a PR") was too big for the 20-minute cap and was cleanly interrupted
note: if right-sized picks still hit the cap, raise the cap in a PR rather than picking smaller forever
